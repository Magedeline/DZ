using Monocle;

namespace Celeste
{
    /// <summary>
    /// Freeze effect for entities.
    /// Temporarily stops entity updates while preserving rendering.
    /// </summary>
    public static class Freeze
    {
        /// <summary>
        /// Create a freeze effect on a target entity.
        /// </summary>
        public static void Create(Entity target, float duration)
        {
            // Stub implementation - does nothing
        }
    }
}
